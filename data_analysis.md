# 📊 คู่มือและโครงสร้างข้อมูลสำหรับการวิเคราะห์ (Data Analysis Guide & Dictionary)

เอกสารนี้รวบรวมโครงสร้างฐานข้อมูล (Database Schema) และความสัมพันธ์ของตารางต่างๆ ในระบบ **Student Activity Attendance System** เพื่อใช้เป็นข้อมูลอ้างอิงสำหรับการนำไปวิเคราะห์ข้อมูล (Data Analysis) หรือนำไปสร้างรายงานสรุปผล (Dashboards / Reports) ครับ

---

## 🗄️ โครงสร้างตารางในฐานข้อมูล (Database Schema)

### 1. ตาราง `students` (ข้อมูลนักเรียน)
เก็บข้อมูลพื้นฐานของนักเรียนที่สามารถนำไปกรอง (Filter) วิเคราะห์แยกตามระดับชั้นและห้องได้
* **id**: `Integer` รหัสอ้างอิงในระบบ (Primary Key)
* **student_id**: `String` รหัสนักเรียน
* **full_name**: `String` ชื่อ-นามสกุล
* **grade**: `String` ระดับชั้น (เช่น M.1, M.2)
* **room**: `String` ห้องเรียน
* **is_active**: `Boolean` สถานะการใช้งาน
* **created_at**, **updated_at**: `DateTime` วันที่สร้างและแก้ไข

### 2. ตาราง `activities` (ข้อมูลกิจกรรมหลัก)
เก็บข้อมูลกิจกรรมในภาพรวม
* **id**: `Integer` รหัสกิจกรรม (Primary Key)
* **name**: `String` ชื่อกิจกรรม
* **start_date**, **end_date**: `Date` ช่วงเวลาที่จัดกิจกรรม
* **status**: `String` สถานะของกิจกรรม (draft, scheduled, active, completed, cancelled)
* **eligible_grades**: `String` ระดับชั้นที่สามารถเข้าร่วมได้

### 3. ตาราง `activity_sessions` (รอบของกิจกรรม)
ใน 1 กิจกรรมหลักอาจแบ่งย่อยเป็นหลายรอบ (Session) เพื่อความละเอียดในการเช็กชื่อ
* **id**: `Integer` รหัสรอบกิจกรรม (Primary Key)
* **activity_id**: `Integer` (Foreign Key เชื่อมไปยัง `activities.id`)
* **name**: `String` ชื่อรอบ (เช่น รอบเช้า, วันที่ 2 รอบบ่าย)
* **date**: `Date` วันที่จัดรอบนั้นๆ
* **start_time**, **end_time**: `Time` เวลาเริ่มต้นและสิ้นสุด
* **status**: `String` สถานะของรอบ

### 4. ตาราง `attendance_records` (บันทึกการเช็กชื่อ) ⭐ *ตารางหลักสำหรับการวิเคราะห์*
เก็บข้อมูลการเข้าร่วมกิจกรรมของนักเรียนแต่ละคน
* **id**: `Integer` รหัสบันทึก (Primary Key)
* **student_id**: `Integer` (Foreign Key เชื่อมไปยัง `students.id`)
* **session_id**: `Integer` (Foreign Key เชื่อมไปยัง `activity_sessions.id`)
* **status**: `String` สถานะ (present, manual)
* **checked_in_at**: `DateTime` เวลาที่กดเช็กชื่อสำเร็จ
* **checked_in_method**: `String` วิธีการเช็กชื่อ (qr, manual)

### 5. ตาราง `audit_logs` (บันทึกประวัติการใช้ระบบ)
เก็บ Log ข้อมูลสำหรับการวิเคราะห์ความปลอดภัยและการใช้งานระบบของแอดมิน/ครู
* **id**: `Integer` (Primary Key)
* **user_id**: `Integer` (Foreign Key เชื่อมไปยังตาราง `users`)
* **action**: `String` ประเภทการกระทำ (เช่น activity_created, student_imported)
* **target_type**, **target_id**: `String` สิ่งที่ถูกกระทำและรหัสอ้างอิง
* **created_at**: `DateTime` เวลาที่เกิดเหตุการณ์

---

## 📈 ไอเดียการนำข้อมูลไปวิเคราะห์ (Analysis Use Cases)

เมื่อนำข้อมูลจากตารางต่างๆ มา Join กัน คุณสามารถวิเคราะห์มิติข้อมูลเหล่านี้ได้:

1. **การวิเคราะห์อัตราการเข้าร่วม (Attendance Rate Analysis):**
   * นำข้อมูลจาก `attendance_records` มาเทียบกับ `students` ทั้งหมด (หรือเทียบตาม `eligible_grades` ของกิจกรรมนั้นๆ)
   * *ประโยชน์:* ทำให้ทราบว่า ระดับชั้นไหน หรือห้องไหน มีเปอร์เซ็นต์การเข้าร่วมกิจกรรมสูงที่สุดหรือต่ำที่สุด

2. **การวิเคราะห์พฤติกรรมช่วงเวลาเช็กชื่อ (Check-in Peak Times):**
   * วิเคราะห์จากฟิลด์ `checked_in_at` เพื่อดูว่านักเรียนสแกน QR Code หนาแน่นที่สุดในช่วงเวลากี่โมง
   * *ประโยชน์:* ช่วยให้ครูวางแผนการปล่อยแถว หรือลดความแออัดในการต่อคิวสแกน QR หน้างานได้

3. **ช่องทางการเช็กชื่อ (Method Comparison):**
   * เปรียบเทียบ `checked_in_method` ระหว่าง **"qr"** (สแกนเอง) กับ **"manual"** (ครูเช็กให้) 
   * *ประโยชน์:* วัดประสิทธิภาพความสำเร็จของการใช้ QR Code หากสัดส่วน Manual สูงเกินไป อาจแปลว่าระบบสแกนมีปัญหาหรือเด็กไม่มีสมาร์ทโฟน

4. **การวิเคราะห์ความสนใจต่อกิจกรรม (Activity Popularity):**
   * รวมยอดการเช็กชื่อของแต่ละ `activity_id`
   * *ประโยชน์:* ดูกิจกรรมที่ดึงดูดนักเรียนได้มากที่สุด เพื่อนำไปปรับปรุงการจัดกิจกรรมในภาคการศึกษาถัดไป

---
*⚠️ **ข้อควรระวังเรื่องความเป็นส่วนตัว (Data Privacy & Security):***
*หากจะดึงข้อมูลนี้ออกไปวิเคราะห์ด้วยเครื่องมือภายนอก (เช่น Tableau, Power BI, Excel) แนะนำให้ **Anonymize (ปกปิดหรือเข้ารหัส)** ข้อมูล `full_name` และ `student_id` ไว้ก่อน เพื่อความปลอดภัยของข้อมูลนักเรียนครับ*
